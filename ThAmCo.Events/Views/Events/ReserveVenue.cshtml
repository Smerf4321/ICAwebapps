@model ThAmCo.Events.Data.Event

@{
    ViewData["Title"] = "ReserveVenue";
}

<h2>ReserveVenue</h2>

<h4>Event</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ReserveVenue">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="Id" type="hidden"/>
            <div class="form-group">
                <label asp-for="venueRef" class="control-label"></label>
                <select asp-for="venueRef" asp-items="ViewBag.venues" class="form-control" ></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
